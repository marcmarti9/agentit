# Reference gate invariant

`TASK_DECISION` owns semantic reference selection. Verification owns mechanical proof that a non-`none` reference decision was actually followed.

No keyword router is permitted between those two layers.
