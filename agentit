#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"

if [[ "${1:-}" == "verify" ]]; then
  shift
  exec python3 "$SCRIPT_DIR/router/verify_cli.py" "$@"
fi

exec python3 "$SCRIPT_DIR/router/profiles.py" "$@"
